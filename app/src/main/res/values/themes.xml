<resources>

    <style name="Theme.LearnIT" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/mint_green</item>
        <item name="colorSurface">@color/iceberg</item>
        <item name="colorOnSurface">@color/black</item>
    </style>

    <style name="Theme.LearnIT.NoActionBar">
        <!-- No action bar theme for splash screen and login -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>